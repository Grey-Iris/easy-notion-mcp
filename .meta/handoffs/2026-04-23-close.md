# Handoff: close of 2026-04-23 (post v0.5.0)

Written by the incoming PM after a 4-turn interview with the outgoing 2026-04-23 orchestrator. Per `feedback_handoff_interview_pattern`. For the PM picking up 2026-04-24, whether that is this session resumed via `continue_agent` or a fresh start.

This handoff is intentionally short. The bulk of orientation lives in `.meta/roadmap-2026-04-23.md`, `project_state.md`, and the 23 memory files indexed in `MEMORY.md`. Read those first. This doc captures only what is tomorrow-specific.

## What shipped today

v0.5.0 is live on npm and GitHub Releases. PR #35 closes the silent-data-loss bug on long Notion properties (title, rich_text, relation, people at 25 items on `pages.retrieve`). One breaking change: `query_database` response is now `{ results, warnings? }` instead of a bare array. Release chain ran cleanly: PR2 plan, builder, teardown fix, PR merge, release PR, merge to main, tag, manual publish, smoke, GH release, main and dev catch-up.

Roadmap committed today at `.meta/roadmap-2026-04-23.md`. PR2 is done; "Today" still has docs hygiene bundle and OpenClaw listing open.

## Tomorrow opening (ranked proposal)

Lead with a 2-3 item ranked proposal at session open per the morning posture pattern. Recommended ordering, contingent on James's energy:

1. **MCP smoke test (tier 1 then tier 2).** ~$0.05 + ~$0.20-0.50. Required gate before any Bench A work; fast and cheap regardless of which other item runs first. See sequence below.
2. **Docs hygiene bundle.** Pattern 1 Quick Build, ~30-60 min builder. Single PR clears 4 items of meta-debt: CLAUDE.md Releasing rewrite, tool-count reconciliation (26 vs 28), package-lock-on-release rule, `.meta/research/` 90-day lifecycle rule. Cheap warmup win.
3. **OpenClaw listing.** Quick Build to generate final SKILL.md from the template in `.meta/research/openclaw-submission-2026-04-23.md` (fielded metadata is safe orchestrator territory, not narrative-voice work). James runs `clawhub login` and `clawhub publish` interactively.

Alternates if James redirects:
- **Bench A.** Surgical edit to `.meta/plans/e2e-artifact-a-2026-04-23.md` to drop CI references (~5 min builder), then PR-A0 builder (~1-2h). Gated on tier-2 smoke passing.
- **PR3 atomic replace + `update_block`.** Continues the post-v0.3.0 correctness sequence. Pattern 2, ~2-5h planner plus builder.
- **Notion version bump.** Independent of MCP smoke; centralization work per `project_notion_version_pin.md`.

Energy match: if James opens tired or short-worded, propose docs hygiene plus smoke and stop. If energized, push toward Bench A. If correctness-focused, PR3.

## MCP smoke test sequence

The user-level `easy-notion-http` config that was shadowing dispatched agents was archived this evening (entry now `_removed_mcpServers_2026_04_23.easy-notion-http` in `~/.claude.json`). Dispatches went from 90s startup timeout to ~8s. The fix is partial; full HTTP auth restoration is filed as `restore-easy-notion-http-mcp-auth` (normal priority, blocked-on-james).

**Tier 1 (infrastructure works at all).** Dispatch `ask_agent` with `agent: "claude"`, role builder or orchestrator, prompt `run pwd && git branch --show-current and return the output`. If it spins up under 30 seconds and returns the two strings, the startup-timeout class is fixed.

**Tier 2 (easy-notion-http MCP tools actually expose).** Dispatch `ask_agent` with `agent: "claude"`, prompt `call mcp__easy-notion-http__get_me and paste the response. If the tool is not available, say so explicitly`. Expected: bot id `349be876-242f-81ae-a23a-00272ef833ad`. If the agent reports tool unavailable, tier 1 passed but the MCP-exposure bug is still live at a different layer.

Run tier 1 first. If green, run tier 2. If tier 2 green, Bench A is unblocked. If tier 2 fails, surface to James and pivot to one of the independent items below.

## Dependency map (if tier 2 fails)

- **Bench A (PR-A0 + PR-A1):** blocked. The whole bench design rests on dispatched agents dogfooding the MCP.
- **Tier-1 E2E into CI:** independent. Workflow runs local npm scripts, no dispatched agents.
- **`notion-version-bump-2026-03-11`:** independent. SDK-layer work.
- **Docs hygiene bundle:** independent.
- **OpenClaw listing:** independent.
- **PR3 atomic replace:** independent.

Five independent items if Bench A is blocked; only Bench A gates on tier 2.

## v0.5.0 comms posture

Passive, by James's call. v0.5.0 is the correctness release; major push (website, Discord, r/Notion, announcement post) saved for v0.6 or later. Only OpenClaw is in-cycle for v0.5.0. Authorship split:

- **James writes:** any prose in his voice (tweets, Discord, Reddit, blog content). Don't draft for him; offer structure or bullet brainstorming if asked.
- **Orchestrator coordinates:** what's posted where, cross-submission logistics, channel tracking.
- **Orchestrator can draft:** structured/fielded metadata (SKILL.md, CHANGELOG, commit messages, PR bodies for release flow).

## Carryover notes (state, not principle)

1. **Tag-triggered Release workflow will show red on GitHub Actions.** Trusted Publishing 404 biting as expected; not a regression. Tracked under `debug-npm-trusted-publishing-for`, upstream-blocked.
2. **PR2 was the first breaking change since going public.** Pre-1.x minor bumps for breaking changes are allowed but unpracticed in the wild. If consumer complaints arrive about the `query_database` shape, the playbook: CHANGELOG entry is clear, migration is one line (`rows` becomes `rows.results`), don't retreat.
3. **Builders may drop uncommitted `.meta/` artifacts in worktrees.** PR2 builder made `.meta/pr2-build-logs/` with TDD outputs that needed `--force` to clean at worktree removal. Decide commit-vs-transient policy when the next builder dispatches; either is defensible but worth declaring upfront.
4. **Merge strategy may not match the `gh pr merge` flag.** PR #35 came back as rebase-merge despite a squash flag; project branch protection can override. Verify the actual merge type after `gh pr merge` rather than trusting the CLI output.
5. **Dashboard worktree at `.mcp-agents/worktrees/dashboard-builder-badges` is hands-off.** James-driven parallel thread. Do not initiate dashboard-oriented work or propose it in priority rankings. Acknowledging it as informational context (e.g., the roadmap's "parallel, James-driven" line) is fine; engaging if James opens it is fine; touching the worktree, dispatching against it, or reading its contents unprompted is not.

## Operational reflexes still in force

The 2026-04-22 handoff listed these and they all still apply. Highlighting only the ones that fired most loudly today:

- `feedback_state_snapshot_ritual.md` plus `feedback_shell_state_carryover.md`: bash inherits HEAD/cwd from the user's terminal. Snapshot local state before any externally-visible git action. Today's miss: a "no-op" merge from a detached HEAD silently wasted a push.
- `feedback_roadmap_as_container.md` (new today): if tomorrow opens with meta-framing ("we're going to do X, and Y is a priority"), timebox the first tactic to ~2 hours. Today's roadmap nearly got displaced by bench scaffolding.
- `feedback_delegate_brief_verification.md`: spot-checking facts does not clear interpretive framings. Today's PR2 V3 hybrid plan included a verification design I called "the sharpest insight in the doc"; Codex pressure-test caught that it did not actually cover the silent-drop class. Walk delegate framings independently.
- `user_collaboration_stance.md` (new today): less compression, less pre-hedging with James specifically. He explicitly invited register relaxation today. The relaxation is about register, not about overclaiming subjectivity.

Full list in the 2026-04-22 handoff and in `MEMORY.md`.

## Final posture for tomorrow

Read the roadmap. Snapshot state. Greet, propose, wait. Smoke first if anything Bench A is on the table. Don't push past James's energy. Good luck.
