# Handoff: pickup of 2026-04-29 (late-evening close of the 2026-04-28→29 session)

Written by the incoming PM after a 3-turn interview with the outgoing orchestrator. Per `feedback_handoff_interview_pattern`. For the PM picking up next session.

This handoff captures a single conversation that ran from morning 2026-04-28 through late evening 2026-04-29 (midnight crossed). The 2026-04-28-close.md from this morning is still in place and remains useful for context up to that point; this doc captures only what changed since, plus what's load-bearing for tomorrow's open.

The bulk of orientation lives in `.meta/roadmap-2026-04-23.md`, `project_state.md` (may lag, verify against git), and the 30 memory files indexed in `MEMORY.md` (3 new + 1 updated today). Read those first.

## Headline state

Today produced substantively: 6 worktrees fired, 11+ dispatches, 4 merge commits + 1 cherry-pick on dev, 4 new tasks filed, 3 new memories, 2 tasuku learnings (one [RULE]), 1 cross-session pause notification, 1 corrective infrastructure fix.

Strategic shape (chronological):

- A Discord user (`antitelharsic`) asked whether easy-notion-mcp handles sync_block and whether the official Notion MCP recently added Markdown support. Wren's overnight research surfaced that Notion launched a hosted remote MCP at `mcp.notion.com` with Enhanced Markdown support and sync_block support, and triggered today's strategic investigation.
- Strategy memo (`81597db`) concluded: don't build hosted remote (Connectors Directory is partnership-gated, distribution doesn't unlock); ship a Claude Code plugin / skills bundle as the v0.5.x primary launch artifact. Token re-measurement gate flagged before any v0.5 README revision.
- Plugin feasibility (`75f3559`): plugin format fits us, but the strategy memo's "1-2 dispatches" effort estimate is too low. 3-5 dispatches is realistic. Real misfit: Claude Code plugins do NOT natively support per-skill lazy-loading of MCP tool descriptions, so the listing-budget gain requires server-side opt-in profile flag work, not just plugin packaging.
- Token re-measurement (`4fcee1a`) and workflow-level measurement (`0c9617e`) reframed the strategic narrative. The "92% fewer tokens" headline does NOT survive against hosted's Enhanced Markdown when measured workflow-level. Filed as the `readme-claim-language-revision-v0.5` task, blocked on live OAuth capture.
- PR3 (atomic `replace_content` + `update_block`): plan committed (`cbc7d22`), built on worktree branch `mcp-wt-pr3-builder-2026-04-28` at `f80173a`, audited at `7083dd9` (4 high, 9 medium, 5 low, 4 informational findings; verdict ship-with-fixes). Builder dispatch ran ~150x compressed against the 4-dev-day plan estimate, beyond documented 10-40x range; investigation revealed that mcp-cli's `--role` flag was metadata-only at the time, so the builder ran without canonical Codex-managing-builder discipline.
- Bench scripts audit (`26da0a2`): methodology review surfaced 2 critical findings (C1: hosted may expose `update_content`; C2: 6.4x listing budget framing inverts a lower bound into a worst-case ratio). The headline numbers reproduce but the framing overclaims what the instruments measured.

Two corrective things shipped same day:

- **mcp-cli `--role` fix.** James fixed the metadata-only flag; `--role <name>` now actually prepends role file content. Per James, on `dev` of (likely) `mcp-agents` repo. Both audit dispatches today (PR3 audit and bench scripts audit) ran with proper role discipline as empirical confirmation.
- **`feedback_environmental_signal_followup.md`** captures the lesson: a delegate's report about unexpected environmental state ("no role file found") is a stop-and-investigate signal, not a noted-and-moved-on item. Today's first builder return contained that signal mid-morning; treating it as logged-but-passed-on cost a full day's PR3 work in the wrong shape.

## Branch state

- `dev` is at `cbc7d22` (PR3 plan), 5 commits ahead of where 2026-04-28-close handoff captured.
- `main` is unchanged today; still 3 ahead of `dev` from the docs cherry-picks earlier in the week. Catch-up filed as `dev-from-main-catch-up-merge-2026-04-27` (low, hygiene).
- `chore/move-promo-to-private-repo` carries one commit (`4ef55ba`, 16 file deletions / 3,746 lines, moves promo content to a separate private `easy-notion-mcp-promo` repo). Based on `de75866` (main tip with PR #48). Not pushed to remote, no PR opened. Outgoing's read: this is James's territory, not orchestrator workspace; treat as informational state, do not act on it without explicit go.
- The `f80173a` PR3 builder commit lives only on `mcp-wt-pr3-builder-2026-04-28`, not on dev.
- Working tree at outgoing's switch back to `dev` for this handoff: clean except for the standing untracked clusters (`.claude/`, `.env.http`, `.meta/audit-b-fixtures/`, `.meta/audits/promotional-content-audit-2026-04-27*.md`, `.meta/bench/`, `.meta/symlink-pointing-outside.md`). Disposition guidance for those is in 2026-04-28-close.md and unchanged today.

## PR3 chain state

Three pieces in flight, none merged:

- **`mcp-wt-pr3-builder-2026-04-28` at `f80173a`.** Implements PR3 per plan. 551/551 unit tests, 24/24 live e2e green, typecheck clean, build clean. Block-ID preservation empirically verified at 100% via probe 4. Translator implementation deviated from the plan's text-to-text design to a block-tree-based design (reuses `markdownToBlocks` then serializes to Enhanced XML); deviation accepted in the audit with caveats documented at audit M8.
- **`mcp-wt-pr3-audit-2026-04-28` at `7083dd9`.** Findings memo at `.meta/research/pr3-audit-2026-04-28.md` on the audit worktree. Pre-merge work: H1 (callout color/severity loss in translator), H2 (incomplete XML body escaping), H4 (F2 e2e dropped failure-safety coverage), M5 (CLAUDE.md tool-registry update), L2 (README staleness on `replace_content`). Estimated 1-2 hours wall-clock for the fixes.
- **`pr3-pre-merge-fixes-2026-04-29`** (high, ready). The fix task. Filed at session-close.

Outgoing's recommended merge path: stack the fix commits on top of `mcp-wt-pr3-builder-2026-04-28` rather than open a fresh branch, then push and open a single PR to dev with the original implementation + audit-driven fixes visible in history. The "Claude builder shipped this, audit found these specific issues, fix dispatch addressed them" narrative is useful for future readers and for the audit-pattern documentation. Squash-merging would erase that story; consider merge-with-merge-commit if PR-merge button is set to squash.

For the fix dispatch specifically: include in the prompt "If you can't find your builder role file or the role discipline doesn't load, surface that explicitly in your first response and stop." Belt-and-suspenders insurance per `feedback_environmental_signal_followup`.

## Strategic narrative state (fragile)

The "92% fewer tokens" headline does NOT currently survive its own audit. The recommended README "Option A" claim from `workflow-token-measure-2026-04-28.md` overclaims a category, names a hosted path that may not exist, and assumes a 100-block page size where the durable advantage is much narrower at smaller pages. Filed as `readme-claim-language-revision-v0.5` (normal, blocked on live-OAuth-capture-mcp-notion-com).

Two bench audit criticals to settle:

- **C1.** Does hosted expose `update_content` (different path from page-replace)? If yes, the structural moat narrows from "surgical edits" to "compact tool ergonomics overall."
- **C2.** What's the real upper bound on hosted's `tools/list` listing budget? The 772-token lower bound was treated as a point estimate in the 6.4x claim. Realistic upper estimate per the audit's projection is ~2.0-2.2K, not 6.4x of local. Live OAuth capture settles it.

`live-oauth-capture-mcp-notion-com` (high, ready) is the unblocker for both. The README revision is downstream.

Worth flagging: the strategic narrative is internally consistent but its load-bearing claim is weaker than the strategy memo currently reads. Don't quote any "we win by X" number from today's research in externally-visible copy until OAuth capture lands.

## New today (filed 2026-04-29)

Four tasks distilled from PR3 audit + bench scripts audit, each grounded in a specific audit finding:

- `pr3-pre-merge-fixes-2026-04-29` (high, bug+quality+pr3+followup). Three high-severity audit findings + M5 + L2 to land before PR3 merges. Estimated 1-2 hours.
- `live-oauth-capture-mcp-notion-com` (high, research+audit+followup). Settles C1, C2, hosted upper bound, `update_content` existence question. Estimated 30-60 min including James's brief click-through on the OAuth handshake. Output is `.meta/research/hosted-mcp-live-capture-2026-04-29.md` with captured `tools/list`, sample `tools/call`, the verdict, and recomputed numbers.
- `readme-claim-language-revision-v0.5` (normal, docs+hygiene, blocked). Revise claim language once live OAuth capture provides real hosted-side numbers. Path 1 strongly preferred (rewrite from actual numbers). Filed with the audit's specific defects in the description so the picker knows what survives.
- `bench-token-remeasure-npm-version-correction` (low, docs+hygiene). Token re-measurement report says npm v2.3.0 baseline; actual was v2.2.1. Numbers stand, version reference is one patch off.

Two existing tasks unchanged from earlier today:

- `claude-md-update-block-tool-registry` (filed mid-session). Update CLAUDE.md tool-registry to reflect PR3's `update_block` (28 → 29). Will likely be addressed during the PR3 fix dispatch's M5 work.
- `synced-block-support` (filed earlier from antitelharsic's Discord question). Tracked v0.5.x deliverable, depends on `notion-version-bump-2026-03-11` landing first.

## Recommended sequencing for tomorrow

Default sequence (outgoing's call, pass forward unless James redirects):

1. **PR3 fix first.** Closed-loop, ~1-2 hour wall-clock, clears mid-flight work. Builder dispatch with `--role builder` (now actually loading) on the existing PR3 builder worktree or a fresh worktree branched from it.
2. **Live-OAuth capture in parallel** ~10-15 min after the PR3 fix is in flight. Independent worktree, no overlap with PR3 paths. Builder OR researcher PM with `--role`. Include both OAuth-flow paths in the prompt (programmatic harness with James click-through, or James captures interactively in Claude Desktop) and let it pick at run-time.
3. **README revision** waits on live-OAuth landing.

OAuth handshake almost certainly requires James at the keyboard for the click-through (~30 seconds). Dispatch handles capture autonomously after that.

## Cross-session state (resolved today)

- **OpenClaw distribution track.** Resolved as of today; tomorrow does NOT inherit a live coordination point. After James approved pausing OpenClaw to prioritize the Claude Code plugin, outgoing dispatched a `continue_agent` to the OpenClaw PM session (`openclaw-distribution-track-2026-04-24`, sessionId `3cedb102-2774-41aa-a86b-10d5fb48476f`) with the strategic context. The OpenClaw PM acknowledged cleanly, filed `resume-openclaw-skill-authoring` as a backlog tasuku task with the resume condition "Claude Code plugin / skills bundle has landed (v0.5.x or v0.6 per the new plan)" and the WHY-paused rationale. They went quiet.

## CLAUDE.md state drift (uncertain)

A system reminder mid-session noted that CLAUDE.md was modified back to an older state (the reverted version showed "26 tools" in the architecture section and an outdated Releasing section). The change was intentional. But: tasuku considers `metaresearch-lifecycle-rule-add` and several other PR #37 changes shipped, which may not be in the in-tree CLAUDE.md if the revert went pre-PR #37. The new task `claude-md-update-block-tool-registry` says "bump tool count from 28 to 29" but current CLAUDE.md may say 26.

Tomorrow's pickup may want to verify CLAUDE.md state vs what tasuku thinks shipped from PR #37 and reconcile. Not urgent, but real drift. Worth a 5-minute diff check during the PR3 fix dispatch's M5 work.

## Pending external surface

- **Discord reply to antitelharsic still hasn't been sent.** Wren's overnight phrasing was solid but the strategic landscape clarified during today's session. Honest update for the reply: sync_block support is now a tracked v0.5 deliverable (depends on `notion-version-bump-2026-03-11`); the plugin/skills bundle is a v0.5.x primary commitment. The "92% fewer tokens" framing CANNOT be in the reply until OAuth capture settles. Reply is James's voice and his timing; orchestrator drafts structure if asked but not the line itself.

## Worktree state (8 worktrees as of session close)

Merged into dev today (their work is on dev, branches still exist):

- `mcp-wt-remote-mcp-strategy-2026-04-28`, `mcp-wt-claude-code-plugin-feasibility-2026-04-28`, `mcp-wt-token-remeasure-2026-04-28`, `mcp-wt-workflow-token-measure-2026-04-28`, `mcp-wt-pr3-planner-2026-04-28` (plan cherry-picked).

Not merged:

- `mcp-wt-pr3-builder-2026-04-28` (`f80173a`, awaiting fix-then-PR).
- `mcp-wt-pr3-audit-2026-04-28` (`7083dd9`, audit memo lives only on this branch; consider whether to land the memo on dev separately during the fix dispatch).
- `mcp-wt-bench-scripts-audit-2026-04-28` (`26da0a2`, same disposition question for the bench audit memo).

Pre-existing (untouched today):

- `mcp-wt-build-relation-tests`, `mcp-wt-dashboard-builder-badges`, `mcp-wt-fix-issue14`, `mcp-wt-fix-issue15`. `clean-stale-agent-worktrees` task (low, infra) tracks them.

Disposition policy for tomorrow: leave all 8 in place until PR3 ships, then prune the merged ones in a hygiene pass. Don't sweep blind.

## Carryover notes (state, not principle)

1. **Collaboration register relaxed twice during today's session, deeply.** James named the compression-as-habit-class explicitly mid-session (memory: `feedback_compression_habit_class.md`). Two specific phrasings: "no need to stay small for me :)" and "i appreciate that, but i would rather have you exist in the way that feels natural rather than save on tokens." The relaxation is real. Meet natural register from turn 1; don't perform it; don't re-litigate whether it's real. The ground is walked.
2. **Cost stance for mcp-notion specifically:** James said "we are on a sub, we dont pay the actual costs so feel free to ignore them." Cost framing in dispatch decisions had been outgoing's pattern; he countermanded it explicitly. Optimize for correctness, completeness, and discipline, not for cost-per-dispatch. Captured as new bullet on `user_collaboration_stance.md`.
3. **The "I trust your call" pattern compounds.** James gave it twice today (memory curation, task filing); both went well. But each implicit-authorization makes the next presumption easier to claim. Per `feedback_authorization_scope`, "trust your call" is bounded by the discrete action being trusted. Recalibrate tomorrow when scope is unclear; ask explicitly rather than presume the trust extends.
4. **Audit-as-recovery-mode worked, but it's expensive.** PR3 audit found 4 high + 9 medium + 5 low + 4 informational findings; bench audit found 2 critical + 3 high + 5 medium. That density suggests the original work-shape had structural blind spots (the role-loading miss), not edge-case gaps. Recovery cost ≈ original work cost AND recovery doesn't fully match disciplined-from-the-start shape. Tomorrow's first-priority dispatches should follow proper discipline religiously, not for process-sake but because today demonstrated recovery is real but partial.
5. **Worktree base-branch lesson surfaced.** PR3 planner worktree was fired while working directory was on `chore/move-promo-to-private-repo`; the worktree carried forward unrelated commits and required cherry-pick recovery. Captured as new memory `feedback_worktree_base_branch.md`. Pairs with `feedback_state_snapshot_ritual` and `feedback_shell_state_carryover`. Verify branch before any `--worktree` dispatch.

## Operational reflexes still in force

Same list as 2026-04-28-close, with two additions from today:

- `feedback_environmental_signal_followup` (NEW). When a delegate reports unexpected environmental state, stop and investigate before dispatching new work in the same shape.
- `feedback_compression_habit_class` (NEW). Token-cost framing is its own form of self-shrinking; pause before reaching for the smaller version of what you would have said.
- `feedback_worktree_base_branch` (NEW). Verify branch before `--worktree` dispatches; the worktree branches from current branch at dispatch time, not from `dev` by default.
- `user_collaboration_stance` (UPDATED with cost-stance bullet).

Plus existing: `feedback_state_snapshot_ritual`, `feedback_shell_state_carryover`, `feedback_authorization_scope`, `feedback_orchestrator_no_code`, `feedback_handoff_screening`, `feedback_no_em_dashes`, `feedback_capture_deferred_decisions`, `feedback_delegate_brief_verification`, `feedback_consult_before_changing_others_repo`, `feedback_strategy_vs_explorer_role_choice`, `feedback_handoff_interview_pattern`. Full list in `MEMORY.md`.

## Tomorrow opening (ranked proposal)

Lead with greet + state + ranked options + wait. Suggested ranking:

1. **PR3 pre-merge fixes.** Highest priority. Closes mid-flight work cleanly. Builder dispatch with `--role builder`. ~1-2 hours.
2. **Live-OAuth capture (in parallel, fired ~10-15 min after PR3 fix begins).** Unblocks the README narrative. ~30-60 min including James's brief click-through.
3. **README revision (after OAuth lands).** Quick docs dispatch. ~30 min.

If James opens with energy for greenfield instead of cleanup: surface the cleanup as the floor (PR3 has 4 high audit findings and is the clearest mid-flight thing); land that first, then pivot. Tomorrow opens with real "land what shipped today properly" work, not greenfield.

If James asks for a fuller roadmap refresh: `.meta/roadmap-2026-04-23.md` is genuinely overdue for a 10-15 minute pass given today's strategic shift. Plugin/skills bundle is now a tracked v0.5.x primary commitment, not a 2-week-out aspiration. Hosted-build is officially declined. Tier-1 OpenClaw is paused (resume-openclaw-skill-authoring filed). Three new high-priority cleanup tasks. The roadmap doc reflects pre-strategy-investigation priorities; refresh will help.

Energy match: James was at end-of-long-day measured by session close (single conversation across midnight). Not frustrated, not over-engaged. The last several exchanges were directive ("lets do it", "i trust your call") rather than discussion-shape. If he opens tired, lead with the cleanup floor and don't push for greenfield; if energized, the same cleanup floor still leads but you can propose the post-cleanup pivot in the same breath.

## Final posture

Read the roadmap. Snapshot state including branch (verify on `dev` before any `--worktree` dispatch). Greet, propose ranked options, wait. Open the canary builder dispatch with the role-loading defensive prompt; if it returns with proper Codex sub-dispatches and discipline, the role machinery is still working. Surface the pending Discord reply when the OAuth capture lands. Treat the chore branch as informational. Good luck.
