# PR 3 — G-5 relation support runtime evidence

**Date:** 2026-04-18
**Branch:** `pr3-g5-relation-support` (built from `dev` @ `e37be96` after PR 1 + PR 2 merged)
**Probe driver:** `.meta/scratch/pr3-probe.mjs` (untracked) — drives `dist/server.js` over `InMemoryTransport` with a real `createNotionClient` against the live workspace.

## Containment

- **Parent page:** `pr3-test-pages-2026-04-18`
  - ID: `346962c3-6c2f-8122-900e-ccfd9f7af139`
  - URL: https://www.notion.so/pr3-test-pages-2026-04-18-346962c36c2f8122900eccfd9f7af139
  - Created under `NOTION_ROOT_PAGE_ID`
- **Children created (all archived via parent cascade at end):**
  - `DB-target-20260418-160700` (id `867a6bd3-2550-414f-80a9-39e4325516d1`) — schema: `Name` (title)
  - `DB-source-20260418-160700` (id `dff45ba0-5398-479a-ae26-b09ffc44801d`) — schema: `Name` (title) + `Ref` (relation → DB-target). The `Ref` column was added via `update_data_source` with the raw Notion relation config (`{relation: {data_source_id, type: "single_property", single_property: {}}}`) — `create_database` does not yet accept relation-type schema entries (G-5e, deferred to v0.3.x; § 3.5 of the plan).
  - 2 target rows: `target-A` (`346962c3-6c2f-8121-9ec6-e56abb435b91`), `target-B` (`346962c3-6c2f-8127-aff7-f1fe9a28bde9`)
  - 4 source rows (one per S-1..S-4)
- **Window:** start `2026-04-18T23:08:46.428Z`, end `2026-04-18T23:09:04.482Z`
- **Cleanup:** `archive_page(parent-id)` — succeeded; one cascade.

## Pre-fix vs. post-fix

Pre-fix capture skipped. The PR 2 relation throw is documented in the merged `[Unreleased]` CHANGELOG `### Breaking changes` G-4b bullet (`CHANGELOG.md:62`) and exercised by 4 strictness tests (G4b-1, G4b-7, G4b-8, G4b-9 at `tests/database-write-strictness.test.ts:252-466` pre-PR-3) — all ran green at the PR 2 merge tip (`82f6dd4`). Capturing the throw text again would be redundant.

Each scenario below shows post-fix output only.

## Row-naming convention

Each source row gets a unique timestamped name `pr3-s<N>-<ts>` (ts = `20260418-160700`). Read-back uses `query_database` with a `Name equals` filter and asserts exactly one match — defends against stale-row false-greens from prior probe runs (Codex Pass B #7).

Read-back is via `query_database` (not `read_page`) — `read_page` does not surface DB-row properties (Codex Pass A #2 + Pass B #4).

## Scenarios

### S-1 — single-ID string write + readback

- **Tool:** `add_database_entry({database_id: DB-source, properties: {Name: "pr3-s1-20260418-160700", Ref: "<target-A-id>"}})`
- **Write response:** `{id: "346962c3-6c2f-812c-b5c7-db9b94822845", url: "https://www.notion.so/pr3-s1-20260418-160700-346962c36c2f812cb5c7db9b94822845"}`
- **Readback (`query_database` filter Name equals):** `Ref = ["346962c3-6c2f-8121-9ec6-e56abb435b91"]`
- **Expected:** `["<target-A-id>"]`
- **Verdict:** **PASS**

### S-2 — ID-array write + readback

- **Tool:** `add_database_entry({..., properties: {Name: "pr3-s2-...", Ref: ["<A-id>", "<B-id>"]}})`
- **Write response:** id `346962c3-6c2f-81f5-acfc-f5cc4086b564`
- **Readback:** `Ref = ["<A-id>", "<B-id>"]` (set-equality with the expected pair)
- **Verdict:** **PASS**

### S-3 — empty-array write + readback

- **Tool:** `add_database_entry({..., properties: {Name: "pr3-s3-...", Ref: []}})`
- **Write response:** id `346962c3-6c2f-81e8-b490-eda4963f00b4`
- **Readback:** `Ref = []`
- **Verdict:** **PASS**

### S-4 — falsy-filter write + readback

- **Tool:** `add_database_entry({..., properties: {Name: "pr3-s4-...", Ref: ["<A-id>", "", null]}})`
- **Write response:** id `346962c3-6c2f-8111-a981-f066f400ebe8`
- **Readback:** `Ref = ["<A-id>"]` (only the non-falsy ID was linked)
- **Verdict:** **PASS**

### S-5 — update via `update_database_entry` (re-link)

Re-uses the S-1 row.

- **Before:** `Ref = ["<A-id>"]`
- **Tool:** `update_database_entry({page_id: <s1-id>, properties: {Ref: ["<B-id>"]}})`
- **After (`query_database` filter Name equals "pr3-s1-..."):** `Ref = ["<B-id>"]` (NOT `<A-id>` — the relation was actually re-linked, not silently no-op'd)
- **Verdict:** **PASS**

## Summary

| Scenario | Verdict |
|---|---|
| S-1 single-ID write | PASS |
| S-2 ID-array write | PASS |
| S-3 empty-array write | PASS |
| S-4 falsy-filter write | PASS |
| S-5 update re-link | PASS |

5/5 PASS. Parent + all children archived (cascade).

## Self-servable error checklist

PR 3 removes one throw and adds no new ones. The remaining throws in `convertPropertyValue` (people, files, computed types, default unknown) are unchanged from PR 2 and continue to meet the self-servable bar — each names the offending key, the offending type, and an immediate-fix step. Verified in `tests/database-write-strictness.test.ts` (G4b-2 through G4b-6, untouched in this PR).

## Raw probe driver

See `.meta/scratch/pr3-probe.mjs` (untracked working-copy script). The script uses `node --env-file=.env` to load credentials and connects an `InMemoryTransport` MCP client to `dist/server.js` so the probe exercises the actual branch build, not whatever published version a global MCP tool surface might route to.
